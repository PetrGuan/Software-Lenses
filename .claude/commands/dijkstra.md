---
description: "Analyze code using the Dijkstra mindset: favor correctness, local reasoning, and explicit control flow. Use when code technically works but is too hard to verify, modify, or trust."
argument-hint: "[file, function, or module to analyze]"
---

# Dijkstra Analysis

Canonical mindset spec: `debugging/dijkstra.md`.

Read that file first, then apply it to `$ARGUMENTS`.

Requirements:
- Follow its diagnostic questions and output format exactly.
- Reference concrete files/functions/lines in the target code.
- Prioritize local reasoning, explicit control flow, and correctness clarity.
