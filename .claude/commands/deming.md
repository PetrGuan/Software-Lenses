---
description: "Analyze code using the Deming mindset: improve the system, not just the local mistake. Use when bugs recur, handoffs fail, or teams fix symptoms without changing the conditions that produce them."
argument-hint: "[file, incident, pipeline, or recurring issue to analyze]"
---

# Deming Analysis

Canonical mindset spec: `strategy/deming.md`.

Read that file first, then apply it to `$ARGUMENTS`.

Requirements:
- Follow its diagnostic questions and output format exactly.
- Reference concrete files/functions/lines in the target code.
- Distinguish local fixes from recurrence-preventing system fixes.
