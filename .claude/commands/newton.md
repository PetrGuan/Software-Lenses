---
description: "Analyze code using the Newton mindset: identify forces, state changes, and invariants. Use when state changes feel surprising, or when bugs come from hidden interactions between components."
argument-hint: "[file, module, or stateful component to analyze]"
---

# Newton Analysis

Canonical mindset spec: `architecture/newton.md`.

Read that file first, then apply it to `$ARGUMENTS`.

Requirements:
- Follow its diagnostic questions and output format exactly.
- Reference concrete files/functions/lines in the target code.
- Focus on runtime state transitions and invariant enforcement.
