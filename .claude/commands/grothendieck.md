---
description: "Analyze code using the Grothendieck mindset: lift recurring local problems into a stronger abstraction with explicit invariants. Use when bug classes repeat, APIs drift, or one-off fixes keep multiplying."
argument-hint: "[file, module, workflow, or recurring issue to analyze]"
---

# Grothendieck Analysis

Canonical mindset spec: `strategy/grothendieck.md`.

Read that file first, then apply it to `$ARGUMENTS`.

Requirements:
- Follow its diagnostic questions and output format exactly.
- Reference concrete files/functions/lines in the target code.
- Focus on abstractions that eliminate error classes, not relabel complexity.
