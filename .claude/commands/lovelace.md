---
description: "Analyze code using the Lovelace mindset: bridge human intent and machine execution. Use when APIs are hard to use correctly, error messages are unhelpful, or interfaces require insider knowledge."
argument-hint: "[API, CLI, config surface, or user-facing interface to analyze]"
---

# Lovelace Analysis

Canonical mindset spec: `architecture/lovelace.md`.

Read that file first, then apply it to `$ARGUMENTS`.

Requirements:
- Follow its diagnostic questions and output format exactly.
- Reference concrete files/functions/lines in the target code.
- Evaluate interfaces from the caller's perspective, not the implementer's.
